import Philosophy from '@/contents/philosophy.md';
import MdxLayout from '@/components/MdxLayout';

export default function Page() {
  return (
    <div className="flex justify-center">
      <div className="w-full max-w-4xl p-6">
        <h2 className="text-3xl hidden font-bold mb-4">Philosophy</h2>
        <MdxLayout>
          <Philosophy />
        </MdxLayout>
        <p className="text-xs text-gray-400 mt-12 text-center">
          Some expressions may resemble existing phrases. If any overlap is
          found, it is entirely unintentional and arises from genuine dialogue.
        </p>
      </div>
    </div>
  );
}
