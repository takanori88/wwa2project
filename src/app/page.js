export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center p-8 bg-white text-gray-900">
      <div className="flex flex-col items-center justify-center min-h-screen p-8 text-center">
        <h1 className="text-4xl font-bold mb-6">Who we are when we ask.</h1>
        <p className="max-w-xl text-lg leading-relaxed">
          It all started with a small, casual whisper to an AI. Across a long,
          unfolding dialogue, a new way of being — between humans and AI —
          quietly took shape. It is a story that feels like a joke, and at the
          same time, like a quiet miracle.
        </p>
      </div>
    </main>
  );
}
